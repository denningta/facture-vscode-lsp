
import { isRecognitionException, NoViableAltException } from 'chevrotain';
import { Diagnostic, LanguagesFeature } from 'vscode-languageserver';
import { TextDocument } from 'vscode-languageserver-textdocument';

export const chevrotainDiagToLSDiag = () => {
	const errors = [];

};

export class FactureValidation {


	public async doValidation(textDocument: TextDocument) {
		const validationResult = [];
		

	}
}
